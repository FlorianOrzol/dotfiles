Manage files on homelab devices via a local mirror — push, fetch, delete, rename, goto.
One action per call: --push <device...|all>, --fetch <device>, --delete <mirror-path...>,
--rename <mirror-path> --rename-to <name>, --goto <mirror-dir>.
